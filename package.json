{
  "name": "bukmatika",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "web:dev": "npm run dev --workspace @bukmatika/web",
    "web:build": "npm run build --workspace @bukmatika/web",
    "web:typecheck": "npm run typecheck --workspace @bukmatika/web",
    "web:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
